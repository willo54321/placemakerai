.vercel

# Environment / secrets — ignore all env files except examples
.env
.env.*
!.env.example

node_modules/
.next/

# Prisma build artifact
tsconfig.tsbuildinfo

# Local embedded dev database cluster
.devdb/

# Local pg_dump snapshots (db:backup:prod)
backups/
